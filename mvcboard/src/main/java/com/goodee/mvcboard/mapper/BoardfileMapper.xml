<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goodee.mvcboard.mapper.BoardfileMapper">
	<insert id="insertBoardfile" parameterType="com.goodee.mvcboard.vo.Boardfile">
		INSERT INTO boardfile (
			board_no, 
			origin_filename, 
			save_filename, 
			filetype, 
			filesize
		) VALUES (
			#{boardNo}, 
			#{originFilename},
			#{saveFilename}, 
			#{filetype}, 
			#{filesize}
		)
	</insert>
	
	<select id="selectBoardfile" parameterType="int" resultType="com.goodee.mvcboard.vo.Boardfile"> <!-- list 내에 들어가는 객체의 타입 -->
		SELECT boardfile_no boardfileNo, origin_filename originFilename, save_filename saveFilename, filetype, filesize 
		FROM boardfile WHERE board_no = #{boardNo}  
	</select>
	
	<delete id="removeBoardfile" parameterType="int">
		DELETE FROM boardfile
		WHERE board_no = #{boardNo}
	</delete>
	
	<delete id="removeBoardfileOne" parameterType="int">
		DELETE FROM boardfile
		WHERE boardfile_no = #{boardfileNo}
	</delete>
	<select id="selectBoardfileCnt" parameterType="int"> <!-- 단일 값을 반환하는 쿼리는 resultType 생략 가능  -->
		SELECT COUNT(*) FROM boardfile
		WHERE board_no = #{boardNo}
	</select>
</mapper>